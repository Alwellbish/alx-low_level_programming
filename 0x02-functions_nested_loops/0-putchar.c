#include "main.h"
/**
 * main - Entry point
 *_putchar- writes the character c to stdout
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
putchar ('x');
for (i=1; i<=0; i++)
{
putchar ('_putchar');
}
putchar('\n');

return (0);
}
